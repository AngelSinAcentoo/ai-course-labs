[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-course-labs"
version = "1.0.0"
description = "Reproducible neural network and transformer course labs"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
